<%= render "shared/banner" %>

<div class="container-fluid">
  <h2 class="home-title">Recent Items</h2>
  <div class="row padded row-margin">
    <% @items.first(4).each do |item| %>
      <%= link_to item_path(item) do %>
      <div class="col-xs-12 col-sm-6 col-md-3 col-lg-3">
        <%= render 'shared/card', item: item %>
      </div>
      <% end %>
    <% end %>
</div>
</div>

<div class="container-fluid bck-white">
  <h2 class="home-title">Snow</h2>
  <div class="row padded row-margin">
    <% @items.where(category: "Snow").first(4).each do |item| %>
      <%= link_to item_path(item) do %>
      <div class="col-xs-12 col-sm-6 col-md-3 col-lg-3">
        <%= render 'shared/card', item: item %>
      </div>
      <% end %>
    <% end %>
  </div>
</div>

<div class="container-fluid">
  <h2 class="home-title">Wind</h2>
  <div class="row padded row-margin">
    <% @items.where(category: "Wind").first(4).each do |item| %>
      <%= link_to item_path(item) do %>
      <div class="col-xs-12 col-sm-6 col-md-3 col-lg-3">
        <%= render 'shared/card', item: item %>
      </div>
      <% end %>
    <% end %>
  </div>
</div>

<div class="container-fluid bck-white">
  <h2 class="home-title">Water</h2>
  <div class="row padded row-margin">
    <% @items.where(category: "Water").first(4).each do |item| %>
      <%= link_to item_path(item) do %>
      <div class="col-xs-12 col-sm-6 col-md-3 col-lg-3">
        <%= render 'shared/card', item: item %>
      </div>
      <% end %>
    <% end %>
  </div>
</div>

<div class="container-fluid">
  <h2 class="home-title">Outdoors</h2>
  <div class="row padded row-margin">
    <% @items.where(category: "Outdoor").first(4).each do |item| %>
      <%= link_to item_path(item) do %>
      <div class="col-xs-12 col-sm-6 col-md-3 col-lg-3">
        <%= render 'shared/card', item: item %>
      </div>
      <% end %>
    <% end %>
  </div>
</div>
